//TODO: App routing

import 'package:get/get.dart';

/*appRoutes() => [
  GetPage(name: '/login',
        page: () => const //TODO: Login Page,
        transition: Transition.native,
        transitionDuration: const Duration(milliseconds: 500),);
];*/
